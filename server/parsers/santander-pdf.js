const pdfParse = require('pdf-parse')

// Santander México PDF — parser stub
async function parseSantanderPDF(buffer) {
  throw new Error('Parser de Santander en construcción — sube el PDF por /uploads/debug-pdf para ver su estructura')
}

module.exports = { parseSantanderPDF }
