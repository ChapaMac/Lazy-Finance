const pdfParse = require('pdf-parse')

// HSBC México PDF — parser stub
async function parseHSBCPDF(buffer) {
  throw new Error('Parser de HSBC en construcción — sube el PDF por /uploads/debug-pdf para ver su estructura')
}

module.exports = { parseHSBCPDF }
