const pdfParse = require('pdf-parse')

// Banorte PDF — parser stub
async function parseBanortePDF(buffer) {
  throw new Error('Parser de Banorte en construcción — sube el PDF por /uploads/debug-pdf para ver su estructura')
}

module.exports = { parseBanortePDF }
