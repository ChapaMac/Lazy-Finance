const pdfParse = require('pdf-parse')

// Scotiabank México PDF — parser stub
async function parseScotiabankPDF(buffer) {
  throw new Error('Parser de Scotiabank en construcción — sube el PDF por /uploads/debug-pdf para ver su estructura')
}

module.exports = { parseScotiabankPDF }
