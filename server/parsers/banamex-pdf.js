const pdfParse = require('pdf-parse')

// Banamex / Citibanamex PDF — parser stub
async function parseBanamexPDF(buffer) {
  throw new Error('Parser de Banamex en construcción — sube el PDF por /uploads/debug-pdf para ver su estructura')
}

module.exports = { parseBanamexPDF }
